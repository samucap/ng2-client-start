import { DataParserPipe } from './data-parser.pipe';

describe('DataParserPipe', () => {
  it('create an instance', () => {
    const pipe = new DataParserPipe();
    expect(pipe).toBeTruthy();
  });
});
